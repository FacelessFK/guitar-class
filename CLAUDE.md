# پلتفرم کلاس آنلاین موسیقی

مارکت‌پلیس آموزش خصوصی موسیقی به‌صورت آنلاین، برای بازار ایران. هنرجو ساز و
استاد را انتخاب می‌کند، اسلات آزاد را رزرو می‌کند، پرداخت می‌کند، کلاس زنده روی
سرور جیتسیِ اختصاصی پلتفرم برگزار می‌شود، و چرخه‌ی تمرین و بازخورد بعد از کلاس
ادامه پیدا می‌کند.

سه نوع جلسه: معارفه‌ی رایگان (۲۰ دقیقه، **یک بار برای هر کاربر**، نه یکی به ازای
هر استاد)، جلسه‌ی تکی، و پکیج ماهانه (۴ جلسه روی یک روز و ساعت ثابت هفتگی).

## پیش از هر کاری

- `docs/architecture.md` — سند طراحی، مرجع «چرا»ی ساختار.
- `docs/deployment.md` — آنچه استقرار واقعاً لازم دارد.
- **`git log` منبع اول است، نه حدس زدن از روی کد.** پیام کامیت‌ها عمداً مفصل‌اند
  و دلیل هر تصمیم غیربدیهی در آن‌هاست. پیش از تغییر دادن چیزی که عجیب به نظر
  می‌رسد، `git log -S "<کد مربوطه>"` بزن — احتمالاً عمدی است.

## دستورها

```bash
pnpm install
pnpm dev:infra                  # پستگرس :5433، ردیس :6380 (عمداً غیرپیش‌فرض)
pnpm db:migrate
pnpm db:seed                    # کاتالوگ + کاربر نمونه — در تولید امتناع می‌کند

pnpm --filter @music/api dev    # API روی :4000/api
pnpm dev:worker                 # پروسه‌ی جدا — بدون آن health می‌گوید degraded
pnpm dev:web                    # فرانت روی :3000

pnpm typecheck                  # هر سه پکیج
pnpm -r test                    # نیازمند پستگرس و ردیس بالا
pnpm db:generate                # پس از تغییر اسکیما، سپس db:migrate
```

`db:seed:catalog` فقط سازها را می‌سازد و هیچ کاربری (امن برای تولید).
`db:admin <شماره>` یک حساب **موجود** را ادمین می‌کند. `verify:storage` آداپتور
S3 را روی باکت واقعی می‌سنجد.

## ساختار

```
apps/api/    NestJS + Drizzle. دامنه‌محور:
             auth، catalog، availability، booking، payment، classroom،
             learning، media، notification، teacher، admin، blog، queue
             main.ts پروسه‌ی API · worker.ts پروسه‌ی وُرکر — جدا
apps/web/    Next.js (App Router). سه دنیای جدا با route group:
             (site) صفحات عمومی SSG/ISR · (app) پشت لاگین · room تمام‌صفحه
             auth/ خارج از هر دو گروه است
packages/    shared — منطق خالص، بدون وابستگی به دیتابیس یا فریم‌ورک
```

`packages/shared` هیچ چیزی از دیتابیس یا نست نمی‌شناسد؛ ورودی داده‌ی ساده، خروجی
داده‌ی ساده. برای همین ۱۰۶ تستش در میلی‌ثانیه اجرا می‌شوند و **فرانت هم مستقیم
مصرفش می‌کند**. منطق دامنه را آنجا بگذار، نه در سرویس نست.

## قواعدی که شکستنشان بی‌صدا است

اینها با تست یا تایپ‌چک گرفته نمی‌شوند و اشتباهشان گران است:

- **زمان‌ها در دیتابیس UTC.** تبدیل به تهران و شمسی فقط در لایه‌ی نمایش
  (`apps/web/src/lib/format.ts`). آفست ایران ثابت `+03:30` است — از ۱۴۰۱ ساعت
  تابستانی ندارد.
- **روز هفته: شنبه = ۰ تا جمعه = ۶.** عمداً با `Date.getDay()` فرق دارد.
- **ساعت روز = دقیقه از نیمه‌شب** (int، ۰ تا ۱۴۳۹)، نه تایپ `time`.
- **مبالغ `BigInt` و به ریال.** هیچ محاسبه‌ی پولی با ممیز شناور. سهم استاد
  **تفریق** می‌شود، جدا حساب نمی‌شود. نمایش به تومان فقط در لایه‌ی نمایش.
- **اسلات‌های آزاد ذخیره نمی‌شوند** — هر بار از روی قوانین و استثناها محاسبه
  می‌شوند.
- **قیمت و درصد کمیسیون موقع رزرو اسنپ‌شات می‌شوند**، نه ارجاع. تغییر قیمت استاد
  نباید روی گذشته اثر بگذارد.
- **دفتر کل فقط افزودنی است.** بازپرداخت سطر منفی است نه ویرایش. جمع ساده‌ی
  `net_amount` هر استاد بدهی واقعی پلتفرم به اوست.
- **شماره‌ی موبایل همیشه E.164**؛ ورودی کاربر از `normalizePhone` رد می‌شود.
- **شمارشی‌ها در دو جا تعریف‌اند** (`packages/shared/src/enums.ts` و
  `apps/api/src/db/schema/enums.ts`) و تست همگامی دارند. هر دو را با هم عوض کن.
- **`SLOT_OCCUPYING_STATUSES` باید همیشه با شرط `WHERE` قید `EXCLUDE` در
  `0001_booking_integrity.sql` یکی بماند.** آن قید تنها لایه‌ی مصون در برابر
  شرط رقابتی است؛ ORM بیانش نمی‌کند.

## پیکربندی

`apps/api/src/config/env.ts` **تنها جایی است که متغیرهای لازم را می‌شناسد.**
متغیر تازه باید همان‌جا ثبت شود، وگرنه نبودنش در تولید بی‌صدا می‌ماند و به
آداپتور جعلی برمی‌گردد. `assertEnvironment()` پیش از `NestFactory.create` در
`main.ts` و `worker.ts` صدا زده می‌شود و **همه‌ی** مشکلات را با هم گزارش می‌کند.

`devCode` فقط با `ALLOW_DEV_LOGIN_CODE="true"` برمی‌گردد. پیش‌فرض خاموش است و در
تولید روشن بودنش جلوی بالا آمدن را می‌گیرد. **به `NODE_ENV` تکیه نکن** — پیش‌فرضِ
شرطِ `NODE_ENV !== "production"` باز است و یک متغیر فراموش‌شده یعنی کد ورود هر
کاربری، از جمله ادمین، در پاسخ یک اندپوینت عمومی.

## احراز هویت

- گارد **سراسری** است؛ مسیر عمومی باید صریحاً `@Public()` بگیرد. فراموشی به سمت
  امن می‌افتد.
- توکن دسترسی ۱۵ دقیقه‌ای، بی‌حالت. **عمداً کوکی نیست** (CSRF).
- توکن تازه‌سازی در **کوکی httpOnly** با `path=/api/auth` است و در بدنه‌ی JSON
  نمی‌آید. هر `fetch` سمت مرورگر باید `credentials:"include"` داشته باشد وگرنه
  نشست با اولین رفرش صفحه می‌پرد — بدون هیچ خطایی.
- چرخش توکن یک پنجره‌ی ~۲۰ ثانیه‌ای بازپخش در ردیس دارد تا دو تب هم‌زمان همان
  جانشین را بگیرند. برداشتنش یعنی کاربر از هر دو تب بیرون می‌افتد.
- ⚠️ API باید روی همان دامنه‌ی ثبت‌شده‌ی فرانت باشد (`api.example.com` کنار
  `example.com`). کوکی `SameSite=Lax` است؛ دامنه‌ی متفاوت یعنی تمدید نشست بی‌صدا
  شکست بخورد و به شکل «کاربرها مدام بیرون می‌افتند» دیده شود.

## پرداخت

تأیید **همیشه** سرور به سرور است. پارامتر `Status` که درگاه به مرورگر می‌دهد در
هیچ تصمیمی خوانده نمی‌شود — آن آدرس عمومی است و هر کسی می‌تواند با `Status=OK`
صدایش بزند. مبلغ مورد انتظار هم در درخواست تأیید فرستاده می‌شود.

**آداپتور درگاه بین «قطعاً ناموفق» و «نمی‌دانم» فرق می‌گذارد.** `FAILED` فقط برای
فهرست بسته‌ی `DEFINITIVE_FAILURES`؛ هر چیز دیگری پرتاب می‌شود تا `settleOrder`
سفارش را `PENDING` بگذارد. **این جهت را برعکس نکن** — `FAILED` یعنی پولِ
گرفته‌شده با جلسه‌ی قطعی‌نشده، و هیچ چیز دیگری سراغش نمی‌رود.

⚠️ **واحد مبلغ زرین‌پال با تراکنش واقعی تأیید نشده.** کد ریال می‌فرستد؛ اگر حساب
روی تومان باشد هر پرداختی کد `-50` می‌گیرد.

## اتاق کلاس

- توکن ورود را **فقط API** صادر می‌کند، بعد از سه بررسی: طرفِ رزرو بودن، وضعیت
  `CONFIRMED`/`IN_PROGRESS`، و بودن داخل پنجره‌ی «۱۰ دقیقه پیش تا ۱۵ دقیقه پس از
  جلسه». خارج از شرایط توکن **اصلاً صادر نمی‌شود** و پاسخ `opensAt` می‌دهد تا
  فرانت شمارش معکوس نشان دهد. `nbf`/`exp` دقیقاً دو لبه‌ی همان پنجره‌اند.
- پاسخ `join` یک `config` دارد که باید **عیناً** به `configOverwrite` داده شود.
  این «حالت موسیقی» است و پردازش‌های صوتی گفتارمحور جیتسی را خاموش می‌کند؛
  دست‌کاری‌اش صدای ساز را خراب می‌کند. روی همان بیلدی تثبیت شده که واقعاً اجرا
  می‌شود، نه از روی حافظه.
- هدفون سیمی الزامی است (بدون حذف اکو، بلندگوی باز فیدبک می‌سازد) و تأخیر
  ۱۵۰–۳۰۰ms یعنی استاد و هنرجو نمی‌توانند هم‌زمان بنوازند — کلاس باید نوبتی
  طراحی شود.

## وُرکر و جاروها

API و وُرکر **دو پروسه‌ی جدا** هستند: از API چند نمونه بالا می‌آید ولی وُرکر یکی
می‌ماند. پنج جارو: `expire-holds`، `close-sessions`، `send-reminders` (هر دقیقه)
و `cleanup-media`، `monthly-payouts` (شبانه).

**جارو، نه جابِ تأخیردار** — قاعده‌ی تثبیت‌شده‌ی پروژه. جابِ گم‌شده یعنی جلسه‌ای که
تا ابد باز می‌ماند یا یادآوری‌ای که هرگز نمی‌رود، و هیچ‌کدام سر و صدا نمی‌کنند.
جارو حقیقت را هر بار از دیتابیس می‌خواند و خودش را ترمیم می‌کند.

هر جارو **ضربان مستقل** دارد و آستانه‌ی کهنگی‌اش جداست (۵ دقیقه برای دقیقه‌ای،
۲۶ ساعت برای شبانه). ضربان مشترک یعنی تا وقتی *یکی* کار می‌کند سیستم سالم گزارش
شود. `GET /api/health` بدترینشان را می‌گوید.

## تست

- تست‌های `apps/api` **یکپارچگی‌اند و پستگرس واقعی می‌خواهند** — قید `EXCLUDE` با
  هیچ ماکی قابل اثبات نیست.
- تست‌ها روی دیتابیس `<name>_test` و ردیس شماره‌ی ۱ اجرا می‌شوند و بین هر تست
  `TRUNCATE` و `FLUSHDB` می‌کنند. دیتابیس تست در اولین اجرا خودش ساخته و مایگریت
  می‌شود. `assertTestDatabase` اجرا روی هر دیتابیس بدون پسوند `_test` را رد
  می‌کند — **این گارد را دور نزن**، بدون آن `pnpm test` دیتابیس توسعه را پاک
  می‌کند.
- ⚠️ vitest باید با SWC ترنسفورم کند (`unplugin-swc`). با esbuild پیش‌فرض،
  فراداده‌ی دکوراتور تولید نمی‌شود و DI نست **بی‌صدا** خراب می‌شود.

## فرانت‌اند

- `apps/web/AGENTS.md` را خودِ `next dev` می‌نویسد و دوباره می‌سازد. حذفش نکن؛
  می‌گوید پیش از کدنویسی مستندات نسخه‌ی نصب‌شده‌ی Next را در
  `node_modules/next/dist/docs/` بخوان.
- **`export const revalidate` باید عدد عینی باشد**، نه ثابت import‌شده — Next
  تنظیمات سگمنت را ایستا می‌خواند و بیلد را با «Invalid segment configuration
  export» رد می‌کند.
- **Tailwind 4: از یوتیلیتی‌های `@theme` استفاده کن** (`text-ink-muted`،
  `bg-accent`، `border-border`). نحو براکتی مثل `text-[--color-ink-muted]` به
  `color:--color-ink-muted` تبدیل می‌شود که CSS نامعتبر است و مرورگر بی‌صدا
  دورش می‌اندازد. **هیچ رنگ خامی در کامپوننت‌ها ننویس** — همه در
  `globals.css` زیر `@theme` است، و کلاس‌های `.card` / `.btn-*` / `.badge-*` /
  `.input` / `.prose-fa` از قبل هستند.
- `packages/shared` بدون پسوند `.js` import می‌کند تا مرورگر بتواند بخواندش؛
  `apps/api` با پسوند `.js` (ESM نود). این تفاوت عمدی است.
- `lib/api.ts` سمت سرور و فقط عمومی · `lib/api-client.ts` سمت مرورگر با توکن و
  تمدید خودکار نشست. یکی‌شان نکن.

## جهت بصری

مینیمال و مدرن: پس‌زمینه سفید، متن `#0F172A`، اکسنت آبی `#2563EB`. توکن‌ها در
`apps/web/src/app/globals.css`. برای سطوح تازه از همین پالت استفاده کن.

**نام برند و لوگو هنوز انتخاب نشده** و فعلاً زیر `hyggemode.com` می‌رود. جهت
بصریِ سطوحی که هنوز طراحی ندارند را خودت انتخاب نکن — گزینه بده.

## بدهی‌های آگاهانه — گزارششان نکن

اینها تصمیم‌اند نه باگ. دوباره کشفشان نکن:

- **ثبت حضور از سمت کلاینت می‌آید و قابل جعل است**، و حالا اثر مالی هم دارد:
  استادی که غیبت کرده می‌تواند بازپرداخت را به سوختن جلسه تبدیل کند. راه‌حل
  واقعی هوک سمت سرور جیتسی است. تا آن موقع `NO_SHOW_TEACHER` در صف بررسی ادمین
  می‌ماند.
- **سیاست `NO_SHOW` (هیچ‌کس نیامد) باز است** — فعلاً می‌سوزد.
- فایل یتیم در باکت · مارک‌داون بلاگ پاک‌سازی نمی‌شود.
- **سرور جیتسیِ تیم (`jitsi.hyggemode.com`) مال ما نیست.** پورت ۵۲۲۲ باز گزارش
  شده و عمداً دست‌نخورده. تمدید گواهی دستی است و **پیشنهاد خودکارسازی نده** —
  دسترسی بین‌المللی سرور به‌زودی فعال می‌شود.
- ⚠️ رکورد A دامنه‌ی `class` را حذف نکن. وایلدکارد نامی را که در درخت DNS
  «موجود» شده پوشش نمی‌دهد.

## سبک کامیت

عنوان **انگلیسی و امری**، بدنه توضیح «چرا» نه «چه». هرگز `Co-Authored-By` یا
«Generated with» اضافه نکن.
