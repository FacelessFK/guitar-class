-- ورود با رمز عبور، کنار ورود با کد پیامکی.
--
-- ستون تهی‌پذیر است و بک‌فیل ندارد: هر حسابی که تا امروز ساخته شده از
-- راه کد پیامکی آمده و رمزی ندارد. تهی یعنی «رمز ندارد» و ورود با رمز
-- برایش رد می‌شود — نه اینکه با رشته‌ی خالی باز شود.
ALTER TABLE "users" ADD COLUMN "password_hash" varchar(200);