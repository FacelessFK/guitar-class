import { redirect } from "next/navigation";

/**
 * `/instruments` — فعلاً فقط یک ریدایرکت.
 *
 * پروتوتایپ تأییدشده صفحه‌ی **جزئیات ساز** را دارد ولی آرت‌بورد
 * «فهرست سازها» ندارد. تا وقتی آن صفحه طراحی و تأیید نشده، ساختنِ یک
 * فهرستِ ابداعی یعنی افزودن UI بدون امضا — پس این مسیر به ساز
 * کانونی می‌رود و آیتم «سازها» در ناوبری تأییدشده سالم می‌ماند.
 *
 * ریدایرکت **موقت** است نه دائمی: صفحه‌ی فهرست در نقشه هست
 * (`docs/nocturne-plan.md`) و با ۳۰۸ مرورگرها و موتورهای جست‌وجو این
 * مسیر را برای همیشه به گیتار کلاسیک می‌چسباندند.
 */
export default function InstrumentsIndexPage() {
  redirect("/instruments/classical-guitar");
}
