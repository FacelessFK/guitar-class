/**
 * پنل استاد.
 *
 * ناوبری اینجا نیست: دیزاین برای نقش استاد یک **پوسته‌ی کامل** دارد نه
 * یک نوار تب درون‌صفحه‌ای، و آن سربرگ در `AppShell` از روی مسیر انتخاب
 * می‌شود. `TeacherNav` قبلی که چهار تب زیر سربرگ هنرجو می‌گذاشت حذف
 * شد.
 *
 * گارد «استاد بودن» اینجا نیست و لازم هم نیست: هر چهار صفحه داده‌شان را
 * از `/api/teacher/*` می‌گیرند و آن اندپوینت‌ها به کاربر بدون پروفایل
 * ۴۰۳ می‌دهند. یک گارد کلاینتی روی این لایوت فقط همان تصمیم را دوباره
 * و این بار بی‌اثر می‌گرفت.
 */
export default function TeacherLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}
